# Git Usage Rules

- Do NOT automatically run `git push` after making changes.
- Always present the changes to the user for review first.
- Only run `git push` when the user explicitly says to do so.
